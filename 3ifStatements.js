// If Statements
if (foo) {
    return bar;
}

// Shorthand Syntax:
if (foo)
    return bar;

// If / Else Statements
if (foo) {
    function1();
} else {
   function2();
}

// Ternary Operator:
foo ? function1() : function2();

// The ternary operator is useful when assigning a value to a variable:
var n = foo ? 1 : 2;

// If / Else If / Else:
if (foo) {
  function();
} else if (bar) {
  function2();
} else {
  function3();
}

